DESCRIPTION = "SWARP software for image warping."
HOMEPAGE = ""
LICENSE = "CLOSED"

DEPENDS = "zlib bzip2 libssl curl"

inherit autotools-brokensep pkgconfig

#PR = "r1"
LIC_FILES_CHKSUM = ""

SRC_URI = "git@bitbucket.org:tildeslash/monit.git;branch=monit-5-34-0"

S = "${WORKDIR}/git"

inherit cmake

do_install_append () {
	chown -R root:root ${D}${prefix}
}

