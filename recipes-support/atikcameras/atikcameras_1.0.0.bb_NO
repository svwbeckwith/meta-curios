DESCRIPTION = "Atik Camera Library
SECTION = "libs"
LICENSE = "CLOSED"

SRC_URI = "file:///home/curios/curios_fsw/lib/libatikcameras.so"

#COMPATIBLE_HOST = "x86_64.*-linux"

INSANE_SKIP:${PN} = "ldflags"
INHIBIT_PACKAGE_STRIP = "1"
INHIBIT_SYSROOT_STRIP = "1"

SOLIBS = ".so"
FILES_SOLIBSDEV = ""

do_install () {
        install -d ${D}${libdir}
        install -m 0755 ${WORKDIR}/libatikcameras.so ${D}${libdir}
}

FILES:${PN} += " \
  ${libdir}/* \
"

